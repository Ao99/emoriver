import 'package:flutter/material.dart';

class InteractionsView extends StatelessWidget {
  final IconData tabIcon = Icons.people_alt;

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.people_alt);
  }
}
